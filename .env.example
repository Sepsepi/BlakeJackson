# Broward Lis Pendens Pipeline Configuration
# This file shows environment variables that can be set for weekly automation

# Output directory for all pipeline files (default: pipeline_output)
BROWARD_OUTPUT_DIR=pipeline_output

# Excel file to integrate results with (optional)
# BROWARD_EXCEL_FILE=/path/to/your/excel/file.xlsx

# Number of days back to search Broward records (default: 7)
BROWARD_DAYS_BACK=7

# ZabaSearch batch size (default: 15)
BROWARD_BATCH_SIZE=15

# Run browsers in headless mode (default: true)
BROWARD_HEADLESS=true

# Maximum retry attempts (default: 3)
BROWARD_MAX_RETRIES=3

# Skip pipeline steps (useful for partial runs)
BROWARD_SKIP_SCRAPING=false
BROWARD_SKIP_PROCESSING=false
BROWARD_SKIP_ADDRESS=false
BROWARD_SKIP_PHONE=false

# Example weekly cron job command:
# 0 9 * * 1 cd /path/to/broward && python pipeline_scheduler.py --weekly-mode --cleanup-old-files
